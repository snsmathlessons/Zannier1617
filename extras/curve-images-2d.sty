\ProvidesPackage{curve-images-2d}[2016/09/26 v1.0 Disegna curve implicite 2d]

\RequirePackage{tikz}
\RequirePackage{xkeyval}

\newenvironment{curva2d}[1][]{% Start of environment
  % Definiamo che si possano passare come parametri
  % axes :: bool, per dire se bisogna metterci gli assi o meno
  % grid :: bool, per dire se bisogna metterci la griglia o meno
  \makeatletter
  \define@boolkey{curve}[c]{axes}[true]{}
  \define@boolkey{curve}[c]{grid}[false]{}
  \makeatother

  \setkeys{curve}{#1}

  \newcommand{\drawcurve}[2][]{
    \makeatletter
    \define@key{disegna}[c]{color}[red]{}

    \makeatother

    \setkeys{disegna}{#1}
    % Codice della curva sotto
  }
  
  \begin{tikzpicture}
    \ifcaxes
    \draw[->] (-2,0) -- (4.2,0) node[right] {$x$};
    \draw[->] (0,-4.2) -- (0,4.2) node[above] {$y$};
    \else\relax\fi
    \ifcgrid
    \draw[very thin, color=gray] (-1.9,-3.9) grid (3.9,3.9);
    \else\relax\fi
    
}
{% End of environment
  \end{tikzpicture}
}



